# Akunuba Web

This directory is reserved for the Akunuba Next.js frontend.

The application should use shared packages from `packages/`, especially the generated API client in `packages/sdk` and reusable components in `packages/ui`.
